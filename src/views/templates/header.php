<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Caixa Eletrônico</title>
    <style>
        /* Adicione estilos básicos */
        body { font-family: Arial, sans-serif; margin: 0; padding: 0; }
        .container { max-width: 800px; margin: 0 auto; padding: 20px; }
        .notification { padding: 10px; margin: 10px 0; border-radius: 5px; }
        .success { background-color: #d4edda; color: #155724; }
        .error { background-color: #f8d7da; color: #721c24; }
        .conta-list { list-style-type: none; padding: 0; }
        .conta-item { padding: 10px; margin: 5px 0; background-color: #f1f1f1; cursor: pointer; }
        .conta-item:hover { background-color: #e1e1e1; }
    </style>
</head>
<body>
    <div class="container">